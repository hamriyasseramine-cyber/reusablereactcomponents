# What is React

React is a JavaScript library created by Meta for building user interfaces. Its core idea is simple: break your interface into small, independent pieces called **components**, then combine them to build a full page. React takes care of updating the screen automatically whenever your data changes, so you don't have to manually touch the DOM.

```jsx
function Welcome() {
  return <h1>Hello, welcome to React!</h1>;
}
```
